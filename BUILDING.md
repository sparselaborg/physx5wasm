# Build Instructions

1. Build the docker image
2. Run the docker container
3. Attach the shell to the docker container
4. Run `sh generate.sh`
5. Run `sh make.sh`
6. Download the built libraries from "src/physx/bin/UNKNOWN/" to the "dist" directory